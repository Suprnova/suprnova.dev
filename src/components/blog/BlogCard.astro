---
const { post } = Astro.props;
---

<article class="blog-wrapper w-2/3 flex flex-col md:flex-row">
  <a
    class="blog flex flex-col md:flex-row bg-secondary-500 rounded-xl transition duration-300 ease-in-out border-solid border-2 border-gray-400"
    href={post.url}
  >
    <img
      class="md:w-1/3 aspect-square rounded-md shadow-md scale-90"
      src={post.frontmatter.image}
      alt={post.frontmatter.title}
    />
    <div class="md:w-2/3">
      <h2 class="text-2xl font-bold text-white !py-2">
        {post.frontmatter.title}
      </h2>
      <p class="text-gray-500 !mx-2 !py-1">{post.frontmatter.date}</p>
      <p class="text-gray-500 !mx-2 !py-2">{post.frontmatter.description}</p>
    </div>
  </a>
</article>

<style>
  .blog-wrapper:hover > .blog {
    @apply scale-95;
  }
</style>
